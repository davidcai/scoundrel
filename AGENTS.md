# AGENTS.md

Guidance for AI coding agents working in this repo.

## Project

See `README.md` for the project description. The full rule set lives in `docs/rules.md` and is the source of truth for gameplay behavior.

## Commands

pnpm is the package manager; run `pnpm install` to set up. There are no runnable scripts at this time.

## Project structure

- `docs/rules.md` — the official rule set (source of truth for gameplay)
- `docs/spec.md` — the implementation spec (full game spec)
- `docs/design-plan.md` — the implementation design plan (55 decisions settled)
- `assets/` — card artwork JPEGs for the deck
- `package.json` / `pnpm-lock.yaml` — pnpm toolchain (no scripts yet)
- `README.md` — project description
