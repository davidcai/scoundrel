/**
 * Scoundrel engine — pure TypeScript game logic.
 *
 * This module holds all game rules, deck composition, RNG, combat math,
 * weapon degradation, potion cap, run-away restriction, win/lose detection,
 * and scoring. It imports zero React/UI code so it runs in Node without a
 * DOM for fast deterministic unit tests.
 *
 * The reducer API is `(state, action) => { state, result }` (see docs/spec.md).
 * Nothing is implemented yet — the toolchain is being scaffolded first.
 */
export {};
