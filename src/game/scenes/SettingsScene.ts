import { createStubScene } from './stubScene';

/** Placeholder settings screen — full implementation lands in stage 7. */
export const SettingsScene = createStubScene('SettingsScene', '/settings', 'settings');
